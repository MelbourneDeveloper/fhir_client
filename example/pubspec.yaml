name: example
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  arborio:
    path: ../../arborio
  fhir_client:
    path: ../../fhir_client
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  split_view: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
